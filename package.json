{
  "name": "js-coverage-demo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:cov": "vitest run --coverage",
    "test:cov:watch": "vitest --coverage",
    "clean": "rimraf coverage .vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "rimraf": "^6.0.1",
    "vitest": "^1.6.0"
  }
}